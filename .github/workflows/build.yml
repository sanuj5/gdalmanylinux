name: Build GDAL Wheel

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
        gdal-version: [3.10.2]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python 3.12
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            python3-dev \
            python3-pip \
            wget \
            curl \
            git \
            unzip \
            libtiff-dev \
            libjpeg-dev \
            libgif-dev \
            libsqlite3-dev \
            libpq-dev \
            libcurl4-openssl-dev \
            libgeos-dev \
            libspatialite-dev \
            libproj-dev \
            libopenjp2-7-dev \
            libwebp-dev \
            libxml2-dev \
            libexpat1-dev \
            libxerces-c-dev \
            libpoppler-dev \
            libpoppler-private-dev \
            libfreexl-dev \
            libhdf5-serial-dev \
            libnetcdf-dev \
            liblzma-dev \
            libzstd-dev \
            cmake \
            ninja-build

      - name: Download and build GDAL from source
        run: |
          wget https://github.com/OSGeo/gdal/releases/download/v3.10.2/gdal-3.10.2.tar.gz
          tar -xzf gdal-3.10.2.tar.gz
          cd gdal-3.10.2
          ./configure
          make -j$(nproc)
          sudo make install
          sudo ldconfig

      - name: Install Python GDAL bindings and build wheel
        run: |
          export CPLUS_INCLUDE_PATH=/usr/local/include
          export C_INCLUDE_PATH=/usr/local/include
          pip install --upgrade pip setuptools wheel
          GDAL_VERSION=3.10.2 pip install GDAL==3.10.2
          GDAL_VERSION=3.10.2 pip wheel GDAL==3.10.2 --wheel-dir dist

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: gdal-wheel-${{ matrix.python-version }}
          path: dist/*.whl